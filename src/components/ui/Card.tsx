/**
 * The card surface.
 *
 * Round 24 consolidated the surfaces into `./Layout`; this module stays as the
 * canonical import path the app already uses, so one implementation serves
 * every screen.
 */
export { Card } from './Layout'
