/**
 * The page heading.
 *
 * Round 24 consolidated the page furniture into `./Layout`; this module stays
 * as the canonical import path the app already uses.
 */
export { PageHeader } from './Layout'
